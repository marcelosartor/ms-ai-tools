# credenciais de qualquer ferramenta, em qualquer nível
# (padrão sem barra casa em todos os subdiretórios)
.env
.env.*
!.env.example

# npm
node_modules/
*.tgz

# manifesto gerado pelo instalador
.ms-ai-tools.json

# harness de desenvolvimento (instruções para o Claude Code neste repo);
# não é produto do pool, não é versionado
CLAUDE.md
